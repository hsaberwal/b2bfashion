# MongoDB (local or Railway MongoDB add-on URI)
MONGODB_URI=mongodb://localhost:27017/b2bfashion

# App URL (for emails/links). Use http://localhost:3000 locally.
NEXTAUTH_URL=http://localhost:3000

# Optional: Email provider for OTP (e.g. Resend, SendGrid). Add when ready.
# EMAIL_API_KEY=
# EMAIL_FROM=

# Forward/Upcoming stock password (for protected section)
FORWARD_STOCK_PASSWORD=your-secret-password

# JWT/secret for sessions (generate with: openssl rand -base64 32)
JWT_SECRET=your-jwt-secret-at-least-32-chars
